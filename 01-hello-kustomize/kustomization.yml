apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yml
  - svc.yml

configMapGenerator:
  - name: hello-kubernetes
    literals:
    - MESSSAGE=Hello, Kubernetes!

